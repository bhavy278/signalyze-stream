# Screenshots

Drop three screenshots here so the main README renders them:

- `viewer.png`   — the Document tab: a PDF open in the viewer (toolbar + a search highlight is a nice touch)
- `analysis.png` — the Analysis tab: document type, parties, key terms, risk flags
- `chat.png`     — the Chat tab: a streamed answer with an expanded "Sources" citation

Tip: capture just the app (crop out the browser tabs/URL bar) for a clean look.
